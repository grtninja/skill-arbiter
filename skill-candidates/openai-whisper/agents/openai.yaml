interface:
  display_name: "Openai Whisper"
  short_description: "Local speech-to-text with the Whisper CLI (no API key)."
  default_prompt: "Use $openai-whisper when the request matches this lane: Local speech-to-text with the Whisper CLI (no API key)."
