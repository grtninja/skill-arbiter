interface:
  display_name: "Canvas"
  short_description: "Present, navigate, snapshot, and hide HTML content on connected OpenClaw nodes."
  default_prompt: "Use $canvas when the request matches this lane: Present, navigate, snapshot, and hide HTML content on connected OpenClaw nodes via the canvas tool, especially when showing dashboards, demos, or debugging canvas URL routing."
