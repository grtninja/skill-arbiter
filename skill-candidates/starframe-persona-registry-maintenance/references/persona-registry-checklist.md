# Persona Registry Checklist

1. Every persona pack has a valid `manifest.persona.json`.
2. `files_in_order` entries resolve without fallback surprises.
3. Loader output includes stable metadata (`persona_id`, `version`, `files`).
4. Changes include tests or verification snippet output.
