interface:
  display_name: "Apple Reminders"
  short_description: "Manage Apple Reminders via remindctl CLI (list, add, edit, complete, delete..."
  default_prompt: "Use $apple-reminders when the request matches this lane: Manage Apple Reminders via remindctl CLI (list, add, edit, complete, delete). Supports lists, date filters, and JSON/pla..."
