interface:
  display_name: "Repo C Policy Gate"
  short_description: "Validate policy files and schema contracts"
  default_prompt: "Use $repo-c-policy-schema-gate to validate Repo C policy files and schema-aligned updates before merges."
