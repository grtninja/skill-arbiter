interface:
  display_name: "STARFRAME Policy Gate"
  short_description: "Validate policy files and schema contracts"
  default_prompt: "Use $starframe-policy-schema-gate to validate STARFRAME policy files and schema-aligned updates before merges."
