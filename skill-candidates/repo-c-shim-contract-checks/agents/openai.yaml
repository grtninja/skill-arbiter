interface:
  display_name: "Repo C Shim Contract"
  short_description: "Enforce REPO_B shim contract and tests"
  default_prompt: "Use $repo-c-shim-contract-checks to enforce REPO_B shim endpoint contract, mock-based test coverage, and fail-closed behavior in Repo C."
