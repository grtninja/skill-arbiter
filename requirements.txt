# Runtime and desktop-shell dependencies for the local NullClaw host app.
# Runtime requirements:
# - Python 3.10+
# - Git
# - psutil (preferred local process inspection)
# - Windows PowerShell only as a fallback for process inspection
# - rg.exe available on the host
pywebview>=5.0
psutil>=5.9
